# TODO - Correcciones Temporizador Pomodoro

## Paso 1: Arreglar `js/pomodoro.js`
- [x] Cerrar la función `updateExternalTimer()` con `}` faltante
- [x] Llamar a `updateExternalTimer(remainingSeconds)` dentro de `tick()`
- [x] Sincronizar badge en `startTimer()`, `pauseTimer()`, `resetTimer()`

## Paso 2: Añadir estilos CSS en `css/style.css`
- [x] Añadir `.pomo-fab-badge` (badge circular sobre el FAB)
- [x] Añadir `.pomo-collapsed-time` (tiempo en header colapsado)

## Paso 3: Verificar funcionamiento
- [ ] Probar que el widget carga sin errores

